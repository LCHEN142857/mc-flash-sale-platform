CREATE TABLE product_primary (
     product_id VARCHAR(10) NOT NULL PRIMARY KEY,
     product_name VARCHAR(20),
     status VARCHAR(2),
     create_time VARCHAR(40),
     update_time VARCHAR(40),
     update_manager VARCHAR(10)
);